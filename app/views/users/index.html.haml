.wrapper
  = render 'shared/admin_sidebar'
  .main-panel
    = render 'shared/admin_dashboard_navbar'
    .content{style: 'margin-top: 0px;'}
      .container-fluid
        = render 'shared/flash_messages'
        .row
          .col-lg-12.col-md-12
            .card
              .card-header.card-header-primary
                %h4.card-title 
                  Users
                  .pull-right
                    %a{:href => new_users_admin_path}
                      %i.fa.fa-plus
                      New
              .card-body.table-responsive
                - unless @users.blank?
                  %table.table.table-hover
                    %thead.text-warning
                      %th= "First#{browser.device.mobile? ? '' : ' Name'}"
                      %th= "Last#{browser.device.mobile? ? '' : ' Name'}"
                      %th Email
                      - unless browser.device.mobile?
                        %th Role
                        %th Customer Name
                        %th Active
                        %td
                    %tbody
                      - @users.each do |user|
                        %tr
                          %td= link_to user.first_name, users_admin_path(user)
                          %td= link_to user.last_name, users_admin_path(user)
                          %td= user.email
                          - unless browser.device.mobile?
                            %td= user.role
                            %td= user.customer_name.blank? ? 'N/A' : user.customer_name
                            %td= user.active? ? 'Yes' : 'No'
                            %td
                              = link_to 'Show', users_admin_path(user), class: 'btn btn-info btn-sm'
                              = link_to 'Edit', edit_users_admin_path(user), class: 'btn btn-primary btn-sm'
                - else
                  %p None
              .card-footer
                =# paginate @users